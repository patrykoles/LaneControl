export type MenuItemPost = {
    name: string;
    description: string;
    category: string;
    currentPrice: number;
}

export type MenuItemGet = {
    id: number;
    name: string;
    description: string;
    category: string;
    currentPrice: number;
}

export type MenuItemUpdate = {
    name: string;
    description: string;
    category: string;
    currentPrice: number;
}